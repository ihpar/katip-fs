class Usul {
  constructor(
    public id: string,
    public name: string,
    public numerator: number,
    public denominator: number,
    public aliases: string[],
  ) { }
}

export default Usul;
